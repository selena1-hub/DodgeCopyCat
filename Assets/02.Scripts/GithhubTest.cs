using UnityEngine;

public class GithhubTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //2(o/\/o}!
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
